<Window x:Class="Tester2_01_GUI.TestConstructorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Конструктор тестов - Tester2_01" 
        WindowState="Maximized"
        WindowStartupLocation="CenterOwner"
        Background="White">
    
    <Window.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Основные настройки теста" FontWeight="Bold" Margin="0,0,0,10">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Margin="5">
                    <Label Content="Название теста:" FontWeight="Normal"/>
                    <TextBox x:Name="TestTitleTextBox" 
                            Text="Новый тест" 
                            FontSize="14" 
                            Margin="0,2"
                            ToolTip="Введите название теста"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Margin="5">
                    <Label Content="Преподаватель:" FontWeight="Normal"/>
                    <TextBox x:Name="TeacherTextBox" 
                            Text="" 
                            FontSize="14" 
                            Margin="0,2"
                            ToolTip="Введите ваше имя"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Margin="5">
                    <Label Content="Группа/Класс:" FontWeight="Normal"/>
                    <TextBox x:Name="GroupTextBox" 
                            Text="" 
                            FontSize="14" 
                            Margin="0,2"
                            ToolTip="Введите группу или класс"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Панель загрузки теста -->
        <GroupBox Grid.Row="1" Header="Загрузка теста" FontWeight="Bold" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="5">
                <Button x:Name="LoadTestButton"
                        Content="📂 Загрузить тест" 
                        Width="150" 
                        Height="30"
                        Background="#1E90FF" 
                        Foreground="White"
                        FontWeight="Bold"
                        Margin="0,0,10,0"/>
                
                <Button x:Name="ResetTestButton"
                        Content="🔄 Сбросить" 
                        Width="100" 
                        Height="30"
                        Background="#FF8C00" 
                        Foreground="White"
                        FontWeight="Bold"
                        Margin="0,0,10,0"/>
                
                <TextBlock x:Name="LoadedTestInfoTextBlock" 
                          VerticalAlignment="Center"
                          FontSize="12"
                          Foreground="Green"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Настройки времени" FontWeight="Bold" Margin="0,0,0,10">
            <StackPanel Margin="5">
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <RadioButton x:Name="NoTimeLimitRadio" 
                            Content="Без ограничения времени" 
                            GroupName="TimeLimit"
                            IsChecked="True"/>
                    
                    <RadioButton x:Name="PerQuestionTimeRadio" 
                            Content="Время на вопрос:" 
                            GroupName="TimeLimit"
                            Margin="20,0,0,0"/>
                    
                    <TextBox x:Name="PerQuestionTimeTextBox" 
                            Width="50" 
                            Margin="5,0,0,0"
                            Text="30"
                            IsEnabled="False"
                            ToolTip="Время в секундах на каждый вопрос"/>
                    
                    <TextBlock Text="сек." Margin="5,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <RadioButton x:Name="WholeTestTimeRadio" 
                            Content="Время на весь тест:" 
                            GroupName="TimeLimit"/>
                    
                    <TextBox x:Name="WholeTestTimeTextBox" 
                            Width="50" 
                            Margin="5,0,0,0"
                            Text="300"
                            IsEnabled="False"
                            ToolTip="Общее время в секундах на весь тест"/>
                    
                    <TextBlock Text="сек." Margin="5,0,0,0" VerticalAlignment="Center"/>
                    
                    <TextBlock x:Name="TimeEstimateTextBlock" 
                            FontSize="11" 
                            FontStyle="Italic"
                            Foreground="Gray"
                            Margin="20,0,0,0"
                            VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="Вопросы теста" FontWeight="Bold" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ListView x:Name="QuestionsListView" 
                         Grid.Row="0" 
                         Margin="5">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="№" Width="40" DisplayMemberBinding="{Binding Id}"/>
                            <GridViewColumn Header="Тип" Width="120" DisplayMemberBinding="{Binding TypeDisplay}"/>
                            <GridViewColumn Header="Вопрос" Width="400" DisplayMemberBinding="{Binding QuestionText}"/>
                            <GridViewColumn Header="Баллы" Width="60" DisplayMemberBinding="{Binding Points}"/>
                        </GridView>
                    </ListView.View>
                </ListView>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,5">
                    <Button x:Name="AddQuestionButton"
                            Content="➕ Добавить" 
                            Style="{StaticResource ActionButtonStyle}"
                            Background="#2E8B57" 
                            Foreground="White"/>
                    
                    <Button x:Name="EditQuestionButton"
                            Content="✏️ Редактировать" 
                            Style="{StaticResource ActionButtonStyle}"
                            Background="#1E90FF" 
                            Foreground="White"/>
                    
                    <Button x:Name="DeleteQuestionButton"
                            Content="🗑️ Удалить" 
                            Style="{StaticResource ActionButtonStyle}"
                            Background="#DC143C" 
                            Foreground="White"/>
                    
                    <Button x:Name="MoveUpButton"
                            Content="⬆️ Вверх" 
                            Style="{StaticResource ActionButtonStyle}"
                            Background="#FF8C00" 
                            Foreground="White"/>
                    
                    <Button x:Name="MoveDownButton"
                            Content="⬇️ Вниз" 
                            Style="{StaticResource ActionButtonStyle}"
                            Background="#FF8C00" 
                            Foreground="White"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="SaveTestButton"
                    Content="💾 Сохранить тест" 
                    Width="150" 
                    Height="40" 
                    FontSize="16"
                    FontWeight="Bold"
                    Background="#32CD32" 
                    Foreground="White"
                    Margin="10"/>
            
            <Button x:Name="SaveAsButton"
                    Content="💾 Сохранить как..." 
                    Width="150" 
                    Height="40" 
                    FontSize="16"
                    FontWeight="Bold"
                    Background="#20B2AA" 
                    Foreground="White"
                    Margin="10"/>
            
            <Button x:Name="CloseButton"
                    Content="❌ Закрыть" 
                    Width="150" 
                    Height="40" 
                    FontSize="16"
                    FontWeight="Bold"
                    Background="#696969" 
                    Foreground="White"
                    Margin="10"/>
        </StackPanel>
    </Grid>
</Window>