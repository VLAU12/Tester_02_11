<Window x:Class="Tester2_01_GUI.TextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактор вопроса с вводом текста" 
        Height="450" 
        Width="600"
        WindowStartupLocation="CenterOwner">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                  Text="Вопрос с вводом текста" 
                  FontSize="18" 
                  FontWeight="Bold"
                  Margin="0,0,0,10"/>

        <GroupBox Grid.Row="1" Header="Текст вопроса:" Margin="0,0,0,10">
            <TextBox x:Name="QuestionTextTextBox" 
                    Height="60" 
                    TextWrapping="Wrap" 
                    VerticalScrollBarVisibility="Auto"
                    Text="Введите текст вопроса..."
                    FontSize="14"
                    ToolTip="Можно использовать пропуски вида _____ для заполнения"/>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Правильный ответ:" Margin="0,0,0,10">
            <TextBox x:Name="CorrectAnswerTextBox" 
                    Height="40" 
                    FontSize="14"
                    Text="Введите правильный ответ..."
                    ToolTip="Ответ, который должен ввести студент"/>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Баллы за вопрос:" VerticalAlignment="Center"/>
            <TextBox x:Name="PointsTextBox" 
                    Width="50" 
                    Margin="10,0,0,0" 
                    Text="1"
                    VerticalAlignment="Center"/>
        </StackPanel>

        <GroupBox Grid.Row="4" Header="Настройки проверки:" Margin="0,0,0,10">
            <StackPanel>
                <CheckBox x:Name="CaseSensitiveCheckBox" 
                         Content="Учитывать регистр букв"
                         Margin="0,5"
                         ToolTip="Если отмечено, 'Ответ' и 'ответ' будут считаться разными"/>
                <TextBlock Text="Пример: 'Париж' и 'париж' будут считаться разными ответами" 
                          FontStyle="Italic" 
                          Foreground="Gray"
                          Margin="0,5"/>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="💾 Сохранить" 
                    Width="100" 
                    Height="35"
                    Background="#32CD32" 
                    Foreground="White"
                    FontWeight="Bold"
                    Click="SaveButton_Click"
                    Margin="0,0,10,0"/>
            <Button Content="❌ Отмена" 
                    Width="100" 
                    Height="35"
                    Background="#DC143C" 
                    Foreground="White"
                    FontWeight="Bold"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>