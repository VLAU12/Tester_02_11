<Window x:Class="Tester2_01_GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tester2_01 - Система тестирования" 
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="LightBlue">
    
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#4A90E2" Offset="0"/>
                <GradientStop Color="#7B68EE" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Tester2_01" 
                      FontSize="36" 
                      FontWeight="Bold" 
                      Foreground="White"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,40"
                      TextAlignment="Center"/>
            
            <TextBlock Text="Система создания и прохождения тестов" 
                      FontSize="16" 
                      Foreground="White"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,30"
                      TextAlignment="Center"/>
            
            <Button x:Name="OpenTestButton"
                    Content="📂 Открыть тест" 
                    Width="250" 
                    Height="50" 
                    FontSize="18"
                    FontWeight="SemiBold"
                    Background="#2E8B57"
                    Foreground="White"
                    BorderThickness="0"
                    Margin="0,10"
                    Click="OpenTestButton_Click"
                    Cursor="Hand">
                <Button.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3"/>
                </Button.Effect>
            </Button>
            
            <Button x:Name="ConstructorButton"
                    Content="🛠️ Конструктор тестов" 
                    Width="250" 
                    Height="50" 
                    FontSize="18"
                    FontWeight="SemiBold"
                    Background="#FF8C00"
                    Foreground="White"
                    BorderThickness="0"
                    Margin="0,10"
                    Click="ConstructorButton_Click"
                    Cursor="Hand">
                <Button.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3"/>
                </Button.Effect>
            </Button>

            <TextBlock Text="Версия 1.0" 
                      FontSize="12" 
                      Foreground="White"
                      HorizontalAlignment="Center"
                      Margin="0,30,0,0"
                      Opacity="0.7"/>
        </StackPanel>
    </Grid>
</Window>