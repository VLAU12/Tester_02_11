<Window x:Class="Tester2_01_GUI.QuestionTypeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выбор типа вопроса" 
        Height="700" 
        Width="860"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <Style x:Key="QuestionTypeButton" TargetType="Button">
            <Setter Property="Height" Value="70"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <StackPanel>
            <TextBlock Text="Выберите тип вопроса:" 
                      FontSize="20" 
                      FontWeight="Bold"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,20"/>
            
            <Button Content="📝 Вопрос с выбором ответа" 
                    Style="{StaticResource QuestionTypeButton}"
                    Click="MultipleChoiceButton_Click"
                    ToolTip="Создать вопрос с несколькими вариантами ответов"/>
            
            <Button Content="🔗 Вопрос на сопоставление" 
                    Style="{StaticResource QuestionTypeButton}"
                    Click="MatchingButton_Click"
                    ToolTip="Создать вопрос на сопоставление элементов"/>
            
            <Button Content="⌨️ Вопрос с вводом текста" 
                    Style="{StaticResource QuestionTypeButton}"
                    Click="TextInputButton_Click"
                    ToolTip="Создать вопрос с текстовым ответом"/>
            
            <Button Content="☑️ Множественный выбор" 
                    Style="{StaticResource QuestionTypeButton}"
                    Click="MultipleChoiceMultiButton_Click"
                    ToolTip="Создать вопрос с несколькими правильными ответами"/>
                    
            <Button Content="❌ Отмена" 
                    Height="40" 
                    FontSize="14"
                    Margin="10,20,10,0"
                    Background="#696969" 
                    Foreground="White"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>