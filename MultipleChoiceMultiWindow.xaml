<Window x:Class="Tester2_01_GUI.MultipleChoiceMultiWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактор вопроса с множественным выбором" 
        Height="500" 
        Width="650"
        WindowStartupLocation="CenterOwner"
        Background="Black">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#000000" Offset="0"/>
                <GradientStop Color="#121212" Offset="0.3"/>
                <GradientStop Color="#1a1a1a" Offset="0.5"/>
                <GradientStop Color="#121212" Offset="0.7"/>
                <GradientStop Color="#000000" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <TextBlock Grid.Row="0" 
                  Text="☑️ Вопрос с множественным выбором" 
                  FontSize="18" 
                  FontWeight="Bold"
                  Foreground="#a3a3a3"
                  Margin="0,0,0,10"
                  HorizontalAlignment="Center">
            <TextBlock.Effect>
                <DropShadowEffect Color="#fa8100" BlurRadius="15" ShadowDepth="0" Opacity="0.3"/>
            </TextBlock.Effect>
        </TextBlock>

        <GroupBox Grid.Row="1" Header="Текст вопроса:" Foreground="White" FontWeight="SemiBold" Margin="0,0,0,10">
            <TextBox x:Name="QuestionTextTextBox" 
                    Height="60" 
                    TextWrapping="Wrap" 
                    VerticalScrollBarVisibility="Auto"
                    Text="Выберите правильные варианты..."
                    FontSize="14"
                    Background="#2a2a2a"
                    Foreground="White"/>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,15">
            <Label Content="Баллы за вопрос:" Foreground="White" VerticalAlignment="Center"/>
            <TextBox x:Name="PointsTextBox" 
                    Width="50" 
                    Margin="10,0,0,0" 
                    Text="2"
                    Background="#2a2a2a"
                    Foreground="White"
                    VerticalAlignment="Center"/>
        </StackPanel>

        <GroupBox Grid.Row="3" Header="Варианты ответов (отметьте правильные):" Foreground="White" FontWeight="SemiBold" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ListView x:Name="OptionsListView" 
                         Grid.Row="0" 
                         Height="150" 
                         Background="#2a2a2a"
                         Foreground="White"
                         Margin="0,0,0,10">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Правильный" Width="80">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsCorrect}"
                                                  HorizontalAlignment="Center"
                                                  Foreground="White"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Вариант ответа" Width="450">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Text}" 
                                                Width="440"
                                                Margin="2"
                                                Background="#2a2a2a"
                                                Foreground="White"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="➕ Добавить вариант" 
                            Width="150" 
                            Height="35"
                            Background="#464646"
                            Foreground="White"
                            FontWeight="SemiBold"
                            Click="AddOptionButton_Click"
                            Cursor="Hand">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="#fa8100"/>
                        </Button.Effect>
                    </Button>
                    <Button Content="🗑️ Удалить выбранный" 
                            Width="150" 
                            Height="35"
                            Margin="10,0,0,0"
                            Background="#464646"
                            Foreground="White"
                            FontWeight="SemiBold"
                            Click="RemoveOptionButton_Click"
                            Cursor="Hand">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="#fa8100"/>
                        </Button.Effect>
                    </Button>
                </StackPanel>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="💾 Сохранить" 
                    Width="100" 
                    Height="40"
                    Background="#464646" 
                    Foreground="White"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Click="SaveButton_Click"
                    Margin="0,0,10,0"
                    Cursor="Hand">
                <Button.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="#fa8100"/>
                </Button.Effect>
            </Button>
            <Button Content="❌ Отмена" 
                    Width="100" 
                    Height="40"
                    Background="#464646" 
                    Foreground="White"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Click="CancelButton_Click"
                    Cursor="Hand">
                <Button.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="#fa8100"/>
                </Button.Effect>
            </Button>
        </StackPanel>
    </Grid>
</Window>