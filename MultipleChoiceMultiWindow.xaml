<Window x:Class="Tester2_01_GUI.MultipleChoiceMultiWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактор вопроса с множественным выбором" 
        Height="500" 
        Width="650"
        WindowStartupLocation="CenterOwner">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                  Text="Вопрос с множественным выбором" 
                  FontSize="18" 
                  FontWeight="Bold"
                  Margin="0,0,0,10"/>

        <GroupBox Grid.Row="1" Header="Текст вопроса:" Margin="0,0,0,10">
            <TextBox x:Name="QuestionTextTextBox" 
                    Height="60" 
                    TextWrapping="Wrap" 
                    VerticalScrollBarVisibility="Auto"
                    Text="Выберите правильные варианты..."
                    FontSize="14"/>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Баллы за вопрос:" VerticalAlignment="Center"/>
            <TextBox x:Name="PointsTextBox" 
                    Width="50" 
                    Margin="10,0,0,0" 
                    Text="2"
                    VerticalAlignment="Center"/>
        </StackPanel>

        <GroupBox Grid.Row="3" Header="Варианты ответов (отметьте правильные):" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ListView x:Name="OptionsListView" 
                         Grid.Row="0" 
                         Height="150" 
                         Margin="0,0,0,10">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Правильный" Width="80">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsCorrect}"
                                                  HorizontalAlignment="Center"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Вариант ответа" Width="450">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Text="{Binding Text}" 
                                                Width="440" 
                                                Margin="2"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>

                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <Button Content="➕ Добавить вариант" 
                            Width="140" 
                            Height="30"
                            Click="AddOptionButton_Click"/>
                    <Button Content="🗑️ Удалить выбранный" 
                            Width="140" 
                            Height="30"
                            Margin="10,0,0,0"
                            Click="RemoveOptionButton_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="💾 Сохранить" 
                    Width="100" 
                    Height="35"
                    Background="#32CD32" 
                    Foreground="White"
                    FontWeight="Bold"
                    Click="SaveButton_Click"
                    Margin="0,0,10,0"/>
            <Button Content="❌ Отмена" 
                    Width="100" 
                    Height="35"
                    Background="#DC143C" 
                    Foreground="White"
                    FontWeight="Bold"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>