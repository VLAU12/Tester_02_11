<Window x:Class="Tester2_01_GUI.TestConstructorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Конструктор тестов" 
        WindowState="Maximized"
        WindowStartupLocation="CenterOwner"
        Background="Black">
    
    <Window.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="140"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#464646"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#000000" Offset="0"/>
                <GradientStop Color="#121212" Offset="0.3"/>
                <GradientStop Color="#1a1a1a" Offset="0.5"/>
                <GradientStop Color="#121212" Offset="0.7"/>
                <GradientStop Color="#000000" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
                
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="⚙️ Основные настройки теста" Foreground="White" FontWeight="Bold" Margin="0,0,0,15">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Margin="5">
                    <Label Content="Название теста:" Foreground="White" FontWeight="Normal"/>
                    <TextBox x:Name="TestTitleTextBox" 
                            Text="Новый тест" 
                            FontSize="14" 
                            Margin="0,2"
                            Background="#2a2a2a"
                            Foreground="White"
                            ToolTip="Введите название теста"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Margin="5">
                    <Label Content="Преподаватель:" Foreground="White" FontWeight="Normal"/>
                    <TextBox x:Name="TeacherTextBox" 
                            Text="" 
                            FontSize="14" 
                            Margin="0,2"
                            Background="#2a2a2a"
                            Foreground="White"
                            ToolTip="Введите ваше имя"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Margin="5">
                    <Label Content="Группа/Класс:" Foreground="White" FontWeight="Normal"/>
                    <TextBox x:Name="GroupTextBox" 
                            Text="" 
                            FontSize="14" 
                            Margin="0,2"
                            Background="#2a2a2a"
                            Foreground="White"
                            ToolTip="Введите группу или класс"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Панель загрузки теста -->
        <GroupBox Grid.Row="1" Header="📂 Загрузка теста" Foreground="White" FontWeight="Bold" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" Margin="5">
                <Button x:Name="LoadTestButton"
                        Content="📂 Загрузить тест" 
                        Width="160" 
                        Height="35"
                        Background="#464646" 
                        Foreground="White"
                        FontWeight="SemiBold"
                        Margin="0,0,10,0"
                        Cursor="Hand">
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="#fa8100"/>
                    </Button.Effect>
                </Button>
                
                <Button x:Name="ResetTestButton"
                        Content="🔄 Сбросить" 
                        Width="120" 
                        Height="35"
                        Background="#464646" 
                        Foreground="White"
                        FontWeight="SemiBold"
                        Margin="0,0,15,0"
                        Cursor="Hand">
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="#fa8100"/>
                    </Button.Effect>
                </Button>
                
                <TextBlock x:Name="LoadedTestInfoTextBlock" 
                          VerticalAlignment="Center"
                          FontSize="12"
                          Foreground="#fa8100"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="⏰ Настройки времени"  Foreground="White" FontWeight="Bold" Margin="0,0,0,15">
            <StackPanel Margin="5">
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <RadioButton x:Name="NoTimeLimitRadio" 
                            Content="Без ограничения времени"
                            Foreground="White" 
                            GroupName="TimeLimit"
                            IsChecked="True"/>
                    
                    <RadioButton x:Name="PerQuestionTimeRadio" 
                            Content="Время на вопрос:" 
                            Foreground="White"
                            GroupName="TimeLimit"
                            Margin="20,0,0,0"/>
                    
                    <TextBox x:Name="PerQuestionTimeTextBox" 
                            Width="50" 
                            Margin="5,0,0,0"
                            Text="30"
                            Background="#2a2a2a"
                            Foreground="White"
                            IsEnabled="False"
                            ToolTip="Время в секундах на каждый вопрос"/>
                    
                    <TextBlock Text="сек." Foreground="#a3a3a3" Margin="5,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <RadioButton x:Name="WholeTestTimeRadio" 
                            Content="Время на весь тест:" 
                            Foreground="White"
                            GroupName="TimeLimit"/>
                    
                    <TextBox x:Name="WholeTestTimeTextBox" 
                            Width="50" 
                            Margin="5,0,0,0"
                            Text="300"
                            Background="#2a2a2a"
                            Foreground="White"
                            IsEnabled="False"
                            ToolTip="Общее время в секундах на весь тест"/>
                    
                    <TextBlock Text="сек." Foreground="#a3a3a3" Margin="5,0,0,0" VerticalAlignment="Center"/>
                    
                    <TextBlock x:Name="TimeEstimateTextBlock" 
                            FontSize="11" 
                            FontStyle="Italic"
                            Foreground="#a3a3a3"
                            Margin="20,0,0,0"
                            VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="📋 Вопросы теста" Foreground="White" FontWeight="Bold" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ListView x:Name="QuestionsListView" 
                         Grid.Row="0" 
                         Margin="5"
                         Background="#2a2a2a"
                         Foreground="White">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="№" Width="40" DisplayMemberBinding="{Binding Id}"/>
                            <GridViewColumn Header="Тип" Width="120" DisplayMemberBinding="{Binding TypeDisplay}"/>
                            <GridViewColumn Header="Вопрос" Width="400" DisplayMemberBinding="{Binding QuestionText}"/>
                            <GridViewColumn Header="Баллы" Width="60" DisplayMemberBinding="{Binding Points}"/>
                        </GridView>
                    </ListView.View>
                </ListView>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,5">
                    <Button x:Name="AddQuestionButton"
                            Content="➕ Добавить" 
                            Style="{StaticResource ActionButtonStyle}"
                            Click="AddQuestionButton_Click">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="#fa8100"/>
                        </Button.Effect>
                    </Button>
                    
                    <Button x:Name="EditQuestionButton"
                            Content="✏️ Редактировать" 
                            Style="{StaticResource ActionButtonStyle}"
                            Click="EditQuestionButton_Click">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="#fa8100"/>
                        </Button.Effect>
                    </Button>
                    
                    <Button x:Name="DeleteQuestionButton"
                            Content="🗑️ Удалить" 
                            Style="{StaticResource ActionButtonStyle}"
                            Click="DeleteQuestionButton_Click">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="#fa8100"/>
                        </Button.Effect>
                    </Button>
                    
                    <Button x:Name="MoveUpButton"
                            Content="⬆️ Вверх" 
                            Style="{StaticResource ActionButtonStyle}"
                            Click="MoveUpButton_Click">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="#fa8100"/>
                        </Button.Effect>
                    </Button>
                    
                    <Button x:Name="MoveDownButton"
                            Content="⬇️ Вниз" 
                            Style="{StaticResource ActionButtonStyle}"
                            Click="MoveDownButton_Click">
                        <Button.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="#fa8100"/>
                        </Button.Effect>
                    </Button>
                </StackPanel>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="SaveTestButton"
                    Content="💾 Сохранить тест" 
                    Width="160" 
                    Height="45" 
                    FontSize="16"
                    FontWeight="SemiBold"
                    Background="#464646" 
                    Foreground="White"
                    Margin="10"
                    Click="SaveTestButton_Click"
                    Cursor="Hand">
                <Button.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="#fa8100"/>
                </Button.Effect>
            </Button>
                      
            <Button x:Name="CloseButton"
                    Content="❌ Закрыть" 
                    Width="160" 
                    Height="45" 
                    FontSize="16"
                    FontWeight="SemiBold"
                    Background="#464646" 
                    Foreground="White"
                    Margin="10"
                    Click="CloseButton_Click"
                    Cursor="Hand">
                <Button.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="3" Color="#fa8100"/>
                </Button.Effect>
            </Button>
        </StackPanel>
    </Grid>
</Window>