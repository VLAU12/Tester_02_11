<Window x:Class="Tester2_01_GUI.TestResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Результаты теста - Tester2_01" 
        Height="600" 
        Width="700"
        WindowStartupLocation="CenterOwner"
        Background="White">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                  Text="Результаты тестирования" 
                  FontSize="24" 
                  FontWeight="Bold"
                  HorizontalAlignment="Center"
                  Margin="0,0,0,20"/>

        <GroupBox Grid.Row="1" Header="Результаты" FontWeight="Bold">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="10">
                    <TextBlock x:Name="ResultsTextBlock" 
                              FontSize="14" 
                              TextWrapping="Wrap"
                              Margin="0,0,0,15"/>
                    
                    <Border BorderBrush="LightGray" BorderThickness="1" Margin="0,0,0,10">
                        <Grid>
                            <ProgressBar x:Name="ResultsProgressBar" 
                                        Height="25" 
                                        Minimum="0" 
                                        Maximum="100"/>
                            <TextBlock x:Name="ProgressTextBlock" 
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"
                                      FontWeight="Bold"
                                      Foreground="Black"/>
                        </Grid>
                    </Border>
                    
                    <TextBlock x:Name="GradeTextBlock" 
                              FontSize="16" 
                              FontWeight="Bold"
                              HorizontalAlignment="Center"
                              Margin="0,10"/>
                    
                    <GroupBox Header="Детальная статистика" Margin="0,15,0,0">
                        <StackPanel>
                            <TextBlock x:Name="DetailsTextBlock" 
                                      FontSize="12" 
                                      TextWrapping="Wrap"/>
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
            </ScrollViewer>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Button x:Name="SaveResultsButton"
                    Content="💾 Сохранить результаты" 
                    Width="160" 
                    Height="35"
                    Background="#1E90FF" 
                    Foreground="White"
                    FontWeight="Bold"
                    Click="SaveResultsButton_Click"
                    Margin="5"/>
            
            <Button x:Name="ViewDetailsButton"
                    Content="🔍 Детальные результаты" 
                    Width="180" 
                    Height="35"
                    Background="#FF8C00" 
                    Foreground="White"
                    FontWeight="Bold"
                    Click="ViewDetailsButton_Click"
                    Margin="5"/>
            
            <Button x:Name="CloseButton"
                    Content="✅ Закрыть" 
                    Width="120" 
                    Height="35"
                    Background="#32CD32" 
                    Foreground="White"
                    FontWeight="Bold"
                    Click="CloseButton_Click"
                    Margin="5"/>
        </StackPanel>
    </Grid>
</Window>